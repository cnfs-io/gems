# frozen_string_literal: true
source "https://rubygems.org"

# Foundation gems (cross-repo path references)
gem "flat_record", path: "../rjayroach/flat_record"
gem "rest_cli",    path: "../rjayroach/rest_cli"

# Local gems
gem "pcs", path: "pcs"
gem "pim", path: "pim"
gem "psm", path: "psm"

group :development, :test do
  gem "rspec", "~> 3.0"
end
