#!/bin/sh
# Generated by pcs for <%= host.fqdn %>; `pcs reconcile` rewrites it.
# Runs as root inside the newly installed system, at the end of the install.
# To add steps, copy pcs's template into the project's templates/ directory.
set -eu
