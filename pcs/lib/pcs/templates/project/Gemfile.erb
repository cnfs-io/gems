# frozen_string_literal: true

source "https://rubygems.org"

gem "pcs"

# Provider gems (future — currently built into pcs)
# gem "pcs-proxmox"      # cluster provider
# gem "pcs-truenas"      # nas provider
